#-------------------------------------------------------------------------------
Docker containers
#-------------------------------------------------------------------------------
docker build -f cont/test/Dockerfile .

docker build -t my-image-name:tag -f cont/test/Dockerfile .

docker images

docker ps

docker logs -f <container_name_or_id> -> real-time ingestion

docker logs <container_name_or_id>

docker builder prune --all -> removes all Docker build cache, forcing the next build to start completely from scratch